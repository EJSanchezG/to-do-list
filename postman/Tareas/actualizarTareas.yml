info:
  name: actualizarTareas
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:8080/api/tareas/1
  body:
    type: json
    data: |-
      {
          "titulo": "Analizar flujo de api-huella - Revisión Final",
          "nombreCategoria": "Trabajo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
